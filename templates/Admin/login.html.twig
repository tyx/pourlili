<!doctype html>
<html lang="fr">
    <head>
        <meta charset="utf-8">
        <meta content="ie=edge" http-equiv="x-ua-compatible">
        <title>Pour Lili, liste de naissance de Lili Barray</title>
        <meta content="Liste de naissance de Adeline Thudo et Timothée Barray pour leur fille Lili Barray" name="description">
        <meta content="width=device-width, initial-scale=1.0" name="viewport">
        <link href="https://unpkg.com/material-components-web@latest/dist/material-components-web.min.css" rel="stylesheet">
        <script src="https://unpkg.com/material-components-web@latest/dist/material-components-web.min.js"></script>
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <link rel="stylesheet" href="/css/admin.css">
    </head>
    <body class="mdc-typography mdc-theme--primary-bg" style="padding: 1rem">
        <div class="mdc-theme--background mdc-elevation--z3" style="max-width: 400px; margin: 1rem auto; padding: 2rem">
            <h1>Pour lili</h1>
            {% if error %}
                <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}
            <form action="{{ path('admin_login') }}" method="post">
                <div class="mdc-text-field mdc-text-field--outlined" style="width: 100%" data-mdc-auto-init="MDCTextField">
                    <input type="text" id="username" name="_username" class="mdc-text-field__input" value="{{ last_username }}"/>

                    <div class="mdc-notched-outline">
                        <div class="mdc-notched-outline__leading"></div>
                        <div class="mdc-notched-outline__notch">
                        <label for="_username" class="mdc-floating-label">Username</label>
                        </div>
                        <div class="mdc-notched-outline__trailing"></div>
                    </div>
                </div>

                <div class="mdc-text-field mdc-text-field--outlined" style="width: 100%" data-mdc-auto-init="MDCTextField">
                    <input type="password" id="password" name="_password" class="mdc-text-field__input"/>
                    <div class="mdc-notched-outline">
                        <div class="mdc-notched-outline__leading"></div>
                        <div class="mdc-notched-outline__notch">
                        <label for="password" class="mdc-floating-label">Password</label>
                        </div>
                        <div class="mdc-notched-outline__trailing"></div>
                    </div>
                </div>

                <input type="hidden" name="_csrf_token"
                    value="{{ csrf_token('authenticate') }}"
                >

                <div>
                <button class="mdc-button mdc-button--unelevated" type="submit">
                    <span class="mdc-button__label">Login</span>
                </button>
                </div>
            </form>
        </div>
        <script>
             window.mdc.autoInit();
        </script>
    </body>
</html>
